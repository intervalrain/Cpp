#include <bits/stdc++.h>
using namespace std;

int main(){
    string s = "GeeksforGeeks";
    cout << "Length of String: " << s.size() << endl;

    s.clear();

    if (s.empty()){
      cout << "String is empty\n";
    }

    return 0;
}

