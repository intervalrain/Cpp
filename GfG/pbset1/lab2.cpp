#include <bits/stdc++.h>
using namespace std;

int main(){
    int temp;
    cout << "Please enter a temperature in Celsius: ";
    cin >> temp;
    cout << temp << " deg Celsius in Fahrenheit is: " << (9.0/5*temp + 32) << endl;

    return 0;
}
