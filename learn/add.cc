#include <iostream>

int main(){
    int v1, v2;
    std::cout << "Please enter two numbers to add: ";
    std::cin >> v1 >> v2;
    std::cout << "The sum of " << v1 << " and " << v2 << " is " << v1 + v2 << std::endl;

    return 0;
}